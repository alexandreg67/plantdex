import { FilterPlantsPipe } from './filter-plants.pipe';

describe('FilterPlantsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlantsPipe();
    expect(pipe).toBeTruthy();
  });
});
